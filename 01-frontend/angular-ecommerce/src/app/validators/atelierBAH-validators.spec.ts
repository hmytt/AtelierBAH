import { AtelierBAHValidators } from './atelierBAH-validators';

describe('AtelierBAHValidators', () => {
  it('should create an instance', () => {
    expect(new AtelierBAHValidators()).toBeTruthy();
  });
});
